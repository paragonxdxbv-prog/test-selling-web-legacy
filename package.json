{
  "name": "legacy-store",
  "version": "1.0.0",
  "private": true,
  "description": "LEGACY - Modern storefront for digital/non-digital products with Firebase Firestore and static deploy on Vercel",
  "scripts": {
    "clean": "rimraf public/assets/js",
    "build": "npm run clean && tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json"
  },
  "devDependencies": {
    "rimraf": "^5.0.5",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "firebase": "^10.12.4",
    "firebase-admin": "^12.5.0"
  }
}
